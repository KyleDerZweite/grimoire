// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: Configure integrations as needed
  // integrations: [],

  // Output static HTML (default)
  output: 'static',
});
