{
  "name": "@grimoire/base-astro",
  "version": "0.1.0",
  "private": true,
  "description": "Base Astro template — copy this to sites/ to create a new site",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "echo 'TODO: Add linting'",
    "clean": "rm -rf dist .astro"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/tailwind": "^6.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@grimoire/ui": "workspace:*"
  }
}
